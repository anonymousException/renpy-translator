
name: Package Application with Pyinstaller

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Package Application
      uses: JackMcKew/pyinstaller-action-windows@python3-10-pyinstaller-5-3
      with:
        path: src
        pypi_url: https://pypi.org
        pypi_index_url: https://pypi.org/simple
        spec: "main.spec"

    - run: cp src/google.source.rst src/dist/windows/main/google.source.rst
    - run: cp src/google.target.rst src/dist/windows/main/google.target.rst
    - run: cp src/deepl.source.rst src/dist/windows/main/deepl.source.rst
    - run: cp src/deepl.target.rst src/dist/windows/main/deepl.target.rst
    - run: cp src/youdao.source.rst src/dist/windows/main/youdao.source.rst
    - run: cp src/youdao.target.rst src/dist/windows/main/youdao.target.rst
    - run: cp src/youdao.source.rst src/dist/windows/main/openai.source.rst
    - run: cp src/youdao.target.rst src/dist/windows/main/openai.target.rst
    - run: cp src/main.ico src/dist/windows/main/main.ico
    - run: cp src/cacert.pem src/dist/windows/main/cacert.pem

    - uses: actions/upload-artifact@v4
      with:
        name: Action-Build
        path: src/dist/windows/main
